#!/usr/bin/node

const Rectangle = require('./4-rectangle');

class Square extends Rectangle {
  constructor (size) {
    super(height);
    if (size > 0) {
      this.size = height;
    }
  }
}
module.exports = Square;
